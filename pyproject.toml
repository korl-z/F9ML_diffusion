[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "korlz-ml"
version = "0.1.0"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
include = ["ml*"]
exclude = ["F9ML_venv*"]