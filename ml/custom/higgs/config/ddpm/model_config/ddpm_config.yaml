#ddpm_config.yaml
# model_name: tinyunet_ddpm
# img_shape: [2, 3, 3] #inchannels, w, h

# network:
#   in_channels: 2
#   time_emb_dim: 64
#   base_channels: 16
#   channel_mults: [1, 2, 2]
#   use_attention_at_level: [False, False, True]

model_name: unet_PIT

network:
  base_dim: 128
  depth: 3
  time_emb_dim: 64

diffuser: 
  timesteps: 1000
  scheduler: "cosine"